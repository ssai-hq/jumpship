# External integrations

Public client adapters and coding-agent skills live here. Integrations use generated public contracts and audience-scoped API or local-broker handles; they never import conductor prompts, receive bearer credentials in agent context, or acquire consent, merge, deploy, arbitrary-write, or customer-validation authority.
