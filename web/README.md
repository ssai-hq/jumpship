# Web application

The web package is a presentation boundary deployed separately from shared and cell runtimes. It consumes the generated TypeScript API/event clients, renders safe projections, and performs direct authenticated browser calls to approved API and evidence endpoints. It is not a proxy, workflow authority, credential store, or raw-customer-data processor.
