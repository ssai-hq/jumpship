#!/bin/sh
set -eu

REPO_ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
"$REPO_ROOT/scripts/dev/python-preflight"
export PYTHONDONTWRITEBYTECODE=1
exec python3 "$REPO_ROOT/scripts/dependency-locks/dependency_locks.py" check "$@"
