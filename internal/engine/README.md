# Deterministic engine

Migration operations, canonical transforms, checkpoints, receipts, resource policy, and corridor-facing ports live here. Engine packages do not invoke models or provider runtimes and do not decide shared workflow authority.
