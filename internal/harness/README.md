# Agent harness

Bounded provider, tool, context, memory, trace, wakeup, and reference-loop policy for isolated cells. The harness receives typed, least-authority capabilities; it cannot provision infrastructure or import direct source/target drivers.
