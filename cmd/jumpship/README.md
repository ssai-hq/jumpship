# Public CLI

Composition root for the generated public API client. It holds no consent, secret, merge, deploy, or direct cell capability; consequential human actions finish only through the exact browser ceremony.
