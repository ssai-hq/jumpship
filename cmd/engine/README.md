# Migration engine

Composition root for deterministic migration operations and operator subcommands. Commands adapt shared engine packages; they do not duplicate transforms, checkpoints, gates, model calls, or provider policy.
